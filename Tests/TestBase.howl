⊐ NUnit.Framework;

‒ ○ TestBase {

    ◠ ┈ o(ㅇ arg)    → Assert.That(arg);
    ◠ ┈ o(⊡ x, ⊡ y)  → Assert.That(x, Is.EqualTo(y));

    #if UNITY_EDITOR
    ◠ ┈ Print(⊡ msg)  → 🍥(msg);
    ◠ ┈ Warn(⊡ msg)   → 🔸(msg);
    #endif

}
