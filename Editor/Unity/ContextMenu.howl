⊐ System.Collections.Generic;
⊐ UnityEditor; ⊐ UnityEngine; ⊐ ADB = UnityEditor.AssetDatabase;
⊐ Sel = UnityEditor.Selection;

⊓ Active.Howl{ ‒ ○ ContextMenu{

    [MenuItem("Assets/☆ Apply symset ", ✗, 0)]
    ∘ ┈ ApplySymset(){
        🍥($"Apply symset");
        ∀ (∙ π ∈ Selected(".howl")) Howl.ReimportFile(π, dry: ✗);
    }

    ∘ 𝕃<ㄹ> Selected(ㄹ filetype)
    → Sel.assetGUIDs.GUIDsToPaths(filetype);

    [MenuItem("Assets/⎚ Use Howl 〜 (╯°□°)╯", ✗, 0)]
    ∘ ┈ UseHowl(){
        ⤴ (!Config.ι.allowImport){
            Warn("(⁎˃ᆺ˂) 〜 Enable import in Howl window first");
            ⮐ ;
        }
        ∀ (∙ π ∈ Sel.assetGUIDs.GUIDsToDirs()){
            ⤴(!π.InHowlPath()){
                Howl.ImportDir(π);
                Log($"Howl scripts @ {π.SourcePath()}");
            }
        }
    }

    [MenuItem("Assets/⎚ Use C# (remove Howl scripts)", ✗, 0)]
    ∘ ┈ UseCSharp(){
        ∀ (∙ π ∈ Sel.assetGUIDs.GUIDsToDirs()){
            ⤴ (π.InHowlPath()){
                Log($"Remove Howl scripts 〜 {π}");
                π.RmDir();
            }
        }
        AssetDatabase.Refresh();
    }

    ∘ ㅇ Log(ㄹ msg){ Debug.Log(msg); ⮐ ✓; }

    ∘ ㅇ Warn(ㄹ msg){ Debug.LogWarning(msg); ⮐ ✗; }

}}
