⊐̥ Active.Howl.Header;
⊐̥ Active.Howl.Body;
⊐̥ Active.Howl.Classifier;

⊓ Active.Howl{
‒ ᴾ ○ Map{

    ‒̥ Map @default = ⌢ Rep[]{

        // C# =======================================================

        // ----------------------------------------------------------
        H("Header")
        +
        k * ("⊐̥", "using static" ),
        k * ("⊐", "using"),
        // ----------------------------------------------------------
        H("Blocks")
        +
        c * ("⊓", "namespace"),
        c * ("○", "class"),
        c * ("⍧", "delegate"),
        c * ("⍥", "enum"),
        c * ("◌", "interface"),
        c * ("⊟", "struct"),
        // ----------------------------------------------------------
        H("Modifiers")
        +
        // Access
        m * ("‒", "public"),
        m * ("◠", "protected"),
        m * ("╌", "internal"),
        m * ⌢ Rep("╍", "protected internal", px: "pri"),
        m * ("▰", "private"),
        //
        m * ⌢ Rep("‒̥", "public static", px: "ps"),
        m * ⌢ Rep("◠̥", "protected static", px: "prs"),
        m * ⌢ Rep("╌̥", "internal static", "⊐̥", px: "is"),
        m * ⌢ Rep("╍̥", "protected internal static", px: "pris"),
        m * ⌢ Rep("▰̥", "private static", px: "pvs"),
        //
        // Common
        m * ("☋", "abstract"),
        m * ("ᴸ", "const"),
        m * ("⁺", "override"),
        m * ("ᴾ", "partial"),
        m * ("⌷", "readonly"),
        m * ("∘", "static"),
        m * ("ᵛ", "virtual"),
        // Uncommon
        // (Extern, Readonly, Unsafe, Volatile
        m * ("🔒", "sealed ", alt: "□͆"),

        // ----------------------------------------------------------
        H("Control flow")
        +
        f * ("⤴", "if", alt: "↱"),    // ⑀
        f * ("⤵", "else", alt: "↳"),  // ⑁
        f * ("⤳", "else if", alt: "↪", px: "elif"), // ⑁
        //
        f * ("∀", "foreach")  ,
        f * ⌢ Rep("∈", "in", π: ✗),
        f * ("⟳", "for", alt: "↻"),
        f * ("⟲", "while", alt: "↺"),
        f * -(Rep)("⤓", "continue;", alt: "↓̲"),
        //
        f * ("⤭", "switch", alt: "X"),
        f * ("⥰", "case", alt: "﹦)"), // alt: "↠"),
        f * -(Rep)("¦", "break;", bridge: ✓),
        //
        x * ("⮐", "return"),
        // NOTE: MostHated aberrations; contribs. from Rettie and Tea
        f * ("↯", "try"),
        f * ("⇤", "catch"),
        f * ("(╯°□°)╯", "throw"),
        //new Rep("(ɔ˘з˘)ɔ", "catch", name: "Got U",  px: "got"),
        //new Rep("(ɔ=3=)ɔ", "catch", name: "Gotcha", px: "gotcha"),
        //new Rep("(ɔówó)ɔ", "catch", name: "WTF!",   px: "wtf"),
        f * ("(˙▿˙)"  , "finally"),
        // ----------------------------------------------------------
        H("Linq")
        +
        f * ("‖", "from", alt: "‖"),
        f * ("¿", "where", alt: "?"),
        f * ("፥", "select", alt: "⁝"),
        // ----------------------------------------------------------
        H("Operators")
        +
        o * ⌢ Rep("→", "=>", bridge: ✓, name: "as (→)"),
        o * ⌢ Rep("☰", "==", bridge: ✓, name: "equals (☰)"),
        o * ⌢ Rep("≠", "!=", bridge: ✓, name: "unequals (≠)"),
        o * ⌢ Rep("≥", ">=", bridge: ✓, name: "greater or equals (≥)"),
        o * ⌢ Rep("≤", "<=", bridge: ✓, name: "lesser or equals (≤)"),
        o * ⌢ Rep("∧", "&&", bridge: ✓, name: "and (∧)"),
        o * ⌢ Rep("∨", "||", bridge: ✓, name: "or (∨)"),
        // NOTE: sidelined pending discussion
        // o * ⌢ Rep("⩜", "&&", alt: "∧̶", ns: ✓),
        // o * ⌢ Rep("⩝", "||", alt: "∨̶", ns: ✓),
        k * ⌢ Rep("⨕", "operator", alt: "/̵", name: "Operator")
                                                * B("⨕ ${1:⨀} ($2)"),
        m * ⌢ Rep("⒠", "public static explicit operator",
                 px: "explicit", name: "Explicit type conversion")
                                                   * B("⒠ $1($2 ⧕)"),
        m * ⌢ Rep("⒤", "public static implicit operator",
                 px: "implicit", name: "Implicit type conversion")
                                                   * B("⒤ $1($2 ⧕)"),
        // ----------------------------------------------------------
        H("Primitives")
        +
        p * - ⌢ Rep("ㅇ", "bool",    ι: ✓),  // ▢ // ◩, , ◫,
        p * - ⌢ Rep("ᆨ", "byte",    ι: ✓),  // ▢ // ◩, , ◫,
        p * - ⌢ Rep("ᆩ", "char",    ι: ✓),  // ▢ // ◩, , ◫,
        p * - ⌢ Rep("ᅮ", "short",   ι: ✓),  // ። // ▫, ▪,  ̻)
        p * - ⌢ Rep("ᆞ", "int",     ι: ✓),  // ። // ▫, ▪,  ̻)
        p * - ⌢ Rep("ᅭ", "long",    ι: ✓),  // ። // ▫, ▪,  ̻)
        p * - ⌢ Rep("ㅅ", "float",   ι: ✓),  // ⊓ // ⊓, ⦜, ⌗
        p * - ⌢ Rep("ㅆ", "double",  ι: ✓),
        p * - ⌢ Rep("ᄍ", "decimal", ι: ✓),
        p * - ⌢ Rep("ㄹ", "string",  ι: ✓),  // ⌞ // ⎅ ⊝ ଽ ⦢
        p * - ⌢ Rep("⊡", "object",   ι: ✓),
        p * ("∙", "var"),

        // ----------------------------------------------------------
        H("Keywords")
        +
        k * ⌢ Rep("╭", "get", px: "get") * B("╭{ $0 }"),
        k * ⌢ Rep("╰", "set", px: "set") * B("╰{ $0 }"),
        s * -(Rep)("✓", "true"),   // ⊨
        s * -(Rep)("✗", "false"),  // ⊭
        k * ("⌢", "new"),
        s * -(Rep)("∅", "null"),
        k * -(Rep)("⦿", "this", "•́"),
        ("┈", "void"),
        ("⋯", "params"),

        // ----------------------------------------------------------
        H("Semantics")
        +
        p * -⌢ Rep("⒜", "Action", name: "Action<>"),
        p * -⌢ Rep("⒡", "Func", name: "Func<>") * B("⒡<${0:R}>"),
        p * -(Rep)("𝕄", "Dictionary", alt: "D"),  // ⺵
        p * -(Rep)("𝕊", "HashSet", alt: "M"),
        p * -(Rep)("𝔼", "IEnumerator", alt: "E"),
        p * -(Rep)("𝕃", "List", alt: "L"),
        p * -(Rep)("√", "Sqrt", alt: "L"),
        p * -(Rep)("∑", "Sum"),
        p * -(Rep)("𝛑", "pi (3.14...)", alt: "π"),
        -(Rep)("±", "Append", "±"),
        -(Rep)("∋", "Contains"),
        -(Rep)("⋺", "ContainsKey", "∋⎯"),
        -(Rep)("∃", "Exists"),
        //-(Rep)("ƪ", "Validate"),
        -(Rep)("⧕", "that", alt: "◁"),
        // ----------------------------------------------------------
        H("Idioms")
        +
        o * ⌢ Rep("⎚", "() =>", alt:"-", π: ✗, name: "Do"),
        o * -(Rep)("⁝", ".Count"),
        o * -(Rep)("❙", ".Length"),
        o * -(Rep)("🝠", ".ToString()", alt:"-"),  // 🜙 ୨
        o * -(Rep)("৴", ".ToArray()"),   // ৴  ୪ 🝠
        o * -(Rep)("ᖾ", ".Value", alt: "v"),   // ৴  ୪ 🝠
        -⌢ Rep("【", "(this,", px: "xargs") * B("【$0)"),
        ("🐰", "log.message =", alt: "[shell]"),
        ("🐤", "log.warning =", alt: "[chick]"),
        ("🦞", "log.error =", alt: "[lbstr]"),

        // NUnit ====================================================

        H("NUnit")
        +
        m * ⌢ Rep("؟", "[Test] public void", px: "test"),
        m * ⌢ Rep("⍜", "[SetUp] public void", px: "setup"),
        m * ⌢ Rep("⍉", "[TearDown] public void", px: "teardown"),
        -(Rep)("ಠᴗಠ", "Assert.Throws") * B("ಠᴗಠ<$1>( ⎚ $0 );"),

        // Unity ====================================================

        H("Unity")
        +
        p * -⌢ Rep("ロ", "GameObject", ι: ✓),  // ◰
        p * -(Rep)("⫙", "Component", alt: "m"),
        // Vectors
        p * -(Rep)("エ", "Transform"),  // 𖼲 ⟁ ⊺ ⏧ ⩀ ⁜ み サ
        p * -(Rep)("ᇅ", "Quaternion"),  // ペ, ᇅ
        p * -⌢ Rep("フ", "Vector2", px: "v2"),
        p * -(Rep)("シ", "Vector3"),
        p * -⌢ Rep("タ", "Vector4", px: "v4"),
        // Points
        p * -⌢ Rep("ト", "Vector2", name: "Point2", px: "p2"),
        p * -⌢ Rep("メ", "Vector3", name: "Point3"),
        p * -⌢ Rep("メ̂", "Vector4", name: "Point4", px: "p4"),
        // Idioms
        -⌢ Rep("《", "gameObject.AddComponent<", π: ✗,
              name: "AddComponent", px: "AddComponent") * B("《$0》"),
        -⌢ Rep("》", ">()", π: ✗, ns: ✓),
        -⌢ Rep("⧼", "GetComponent<", π: ✗, alt: "<",
              name: "GetComponent", px: "GetComponent") * B("⧼$0⧽"),
        -⌢ Rep("⧽", ">()", π: ✗, ns: ✓),
        m * ⌢ Rep("⏚","[UnityTest] public IEnumerator", alt: "↓",
                                                       px: "utest"),
        k * ⌢ Rep("⏰","yield return new WaitForSeconds",
                                          alt: "⍉", px: "yieldsec"),
        // Logging (provisional)
        -⌢ Rep("🍥", "UnityEngine.Debug.Log", alt: "﹫",
            px: "log") * B("🍥($\"$0\");"),
        -⌢ Rep("🔺", "UnityEngine.Debug.LogError", alt: "▲",
            px: "err") * B("🔺($\"$0\");"),
        -⌢ Rep("🔸", "UnityEngine.Debug.LogWarning", alt: "◇",
            px: "warn") * B("🔸($\"$0\");"),
        // Active Logic =============================================
        H("Active Logic")
        +
        p * ("⑂", "status"),
        p * ("▷", "public action"),
        p * ("▶", "private action"),
        s * -(Rep)("◇", "done()"),
        s * -(Rep)("☡", "cont()"),
        s * -(Rep)("■", "fail()"),
        k * -(Rep)("⌽", "return @void();")

        // ==========================================================

    };

}}
