
‒ ○ Snippet{

    ‒ ㄹ name, prefix, body;

    ⒤ Snippet((ㄹ name,
                                             ㄹ prefix,
                                             ㄹ body) ⧕)
    → ⌢ Snippet()
    { name = ⧕.name, prefix = ⧕.prefix, body = ⧕.body};

    ⁺‒ ㄹ ToString()
    → $"{name}: {prefix} => |{body}|";

}
