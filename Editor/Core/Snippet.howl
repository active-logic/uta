
‒ ○ Snippet{

    ‒ ㄹ name, prefix, body;

    public static implicit operator Snippet((ㄹ name,
                                             ㄹ prefix,
                                             ㄹ body) ⧕)
    → ⌢ Snippet()
    { name = ⧕.name, prefix = ⧕.prefix, body = ⧕.body};

    ⁺ ‒ ㄹ ToString()
    → $"{name}: {prefix} => |{body}|";

}
