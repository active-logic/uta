⊓ Active.Howl{
‒̥ ○ Requirements{

    ‒̥ ㅇ hasIDE  → atom.Exists() ∨ vscode.Exists();
    ‒̥ ㅇ hasExt  → atom.SupportsHowl() ∨ vscode.SupportsHowl();
    ‒̥ ㅇ hasRoot → Path.FindHowlRoot() ≠ ∅;
    ‒̥ ㅇ mayImport → ✗;
    ‒̥ ㅇ hasVCS → FileSystem.FindInParent(Path.howlRoot, ".git") ≠ ∅;

    ∘ Atom   atom   → ⌢ Atom();
    ∘ VSCode vscode → ⌢ VSCode();

}}
